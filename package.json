{
	"name": "w-c",
	"version": "1.0.0",
	"description": "A static site hosted on Cloudflare Pages.",
	"main": "index.js",
	"scripts": {
		"dev": "wrangler pages dev public --do ROOM=Room@rtc-signaling",
		"do": "wrangler dev do/index.js",
		"npx-dev": "npx wrangler pages dev public --do ROOM=Room@rtc-signaling",
		"npx-do": "npx wrangler dev do/index.js",
		"deploy": "wrangler deploy do/index.js && git push origin main",
		"test": "echo \"Error: no test specified\" && exit 1",
		"html-data": "node build-html-data.mjs"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20241218.0",
		"wrangler": "^3.107.2"
	}
}
